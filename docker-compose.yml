name: outline-authentik-connector
services:
  outline-authentik-connector:
    # Pin to a specific release tag. Bump deliberately after reviewing release
    # notes; do not use :latest in production.
    image: burritosoftware/outline-authentik-connector:v1.2
    ports:
      - 8430:80
    env_file: ".env"
