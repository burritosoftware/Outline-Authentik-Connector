# Fill these in AFTER you create the webhook in the Outline UI (step 3 in README.md).
# Until then, the connector container will refuse to start — that's the M3
# startup-validation fix proving itself.
OUTLINE_TOKEN=
OUTLINE_WEBHOOK_SECRET=
